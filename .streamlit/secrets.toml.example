# Copy to .streamlit/secrets.toml

# --- Login Option A: single user ---
APP_USERNAME = "hr"
APP_PASSWORD = "your-secure-password"

# --- Login Option B: admin + hr (comment out Option A) ---
# [users.admin]
# password = "admin-pass"
# roles = ["admin", "hr"]
#
# [users.hr]
# password = "hr-pass"
# roles = ["hr"]

# Required for "تذكرني" — especially with multiple users (30-day cookie)
APP_REMEMBER_SECRET = "long-random-string-for-remember-me-cookies"

GEMINI_API_KEY = "your-gemini-api-key"

# Email (Gmail example — use App Password)
SMTP_HOST = "smtp.gmail.com"
SMTP_PORT = 587
SMTP_USER = "hr@yourcompany.com"
SMTP_PASSWORD = "your-app-password"
SMTP_FROM = "DABOUQ HR <hr@yourcompany.com>"
HR_EMAIL = "hr@yourcompany.com"

# Public URL for candidate accept/reject links
APP_BASE_URL = "https://your-app.streamlit.app"
